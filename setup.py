#!/usr/bin/env python3

from distutils.core import setup

setup(name='turnkey-temp',
    version='1.0',
    description='A small temp file library',
    author='Stefan Davis',
    url='https://github.com/turnkeylinux/turnkey-temp',
    packages=['temp'])
